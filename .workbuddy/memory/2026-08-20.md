# 2026-08-20 工作日志

## README 补强：新电脑从零环境准备
- 用户确认新电脑可能无环境，评估 README 是否够用 → 发现缺口：未写 **Node.js 版本要求**（Vite 5 需 ≥18，推荐 20 LTS/22）、未明说 Git 前置、未讲清 HBuilderX 仅打包才需、无 `.nvmrc`。
- 已在 README 新增「新电脑从零环境准备（必看）」章节（必装 Git/Node≥18 + 仅打包才需 HBuilderX v5.24 + 装完验证命令），快速开始补前置说明，仓库说明补 `covers/` 被 ignore 且 App 不引用的解释。
- 新增仓库根 `.nvmrc`（写 `20`）锁 Node 版本。已本地提交（`9431015` 之后新提交）。
- 核实事据：`app/package.json` vite ^5.2.0 / @dcloudio 3.0.0-5020420260813003 / vue ^3.4.21；App 封面走 `app/static/xiyouji_cover.png`（已入库），根 `covers/` 不被 App 引用，clone 缺失不影响运行。

## 返回键规范化（navigateBack）
- **问题**：所有自定义 topbar 页面的 `back()` 都写死 `uni.switchTab` 跳首页/我的，从 banner `navigateTo` 进来的页面（如详情页）点返回会跳走而非返回上一级，不规范。
- **修复**：新增 `src/utils/nav.js`，导出 `back(fallbackUrl='/pages/home/home')`——导航栈有上一页（`getCurrentPages().length>1`）时 `uni.navigateBack()` 回上一级，否则回退到兜底 tab（防卡死）。
- **接入口（9 个自定义 topbar 页）**：`import { back as backTo } from '@/utils/nav'`，`back()` 改调 `backTo('/pages/home/home')`（detail/discover/play/search，来自首页体系）或 `backTo('/pages/me/me')`（downloads/points/settings/tasks/vip，来自"我的"体系）。
- **未动**：order/pay/unlock 本就用 `navigateBack`（始终经 navigateTo 进入，无需兜底）；detail 的 `onCh`（点章节进 play）与 search 的 `tapRes`（选结果进 play）是正常跳转逻辑，非返回键，保持 `switchTab`。
- **tabBar 确认**：home/play/vip/me 为 tab；play、vip 经 `switchTab` 进入（无返回栈），兜底分别回 home/me，与现状一致。
- 本地编译通过（"Project 'app' compiled successfully."），重打 APK 成功（云端证书 type3，10:09 完成），已下载覆盖 `app/unpackage/release/apk/diansheng_release.apk`（15.87MB，校验 PK 合法）。

## 工程 git 化（跨机续作准备）
- 已 `git init` + 首次提交 `0c6f732`（分支 master，.git 仅 2.2MB），暂存 52 文件（app/src 全源码 + app/static 资源 + .workbuddy/memory 记忆 + 顶层 *.html 交付物 + 配置）。
- `.gitignore` 忽略：`node_modules/`、`node_modules_bak*/`、`unpackage/`、`dist/`、`*.log`、`covers/`；保留 `app/src`、`app/static`、`.workbuddy/memory`、顶层 `*.html`、manifest/pages/package 等配置。
- 清理：删掉 `app/dist` 与一堆调试 `*.log`；`node_modules_bak`/`node_modules_bak2` 因安全删除层要求批量确认（各 ~4 万文件）未能自动删，已加 gitignore 忽略（不进版本库，不影响迁移；如要本机彻底清可在文件管理器手动删）。
- **待办**：用户选 GitHub 托管，需提供「空仓库 HTTPS URL + Personal Access Token(repo 权限)」后由我 `git remote add origin <url>` + `git push -u origin master`。push 后我会把 token 从 `.git/config` 移除（改回纯 URL），用户日后在新机器用自己的 token。
- **跨机续作链路（新机器 clone 后）**：`npm install` 重建依赖 → 装同版本 HBuilderX + 登录同 DCloud 账号(AppId `__UNI__38009BA` 跟账号走) → 系统级 PATH 加 node(一次性 admin 操作) → 即可续作/重打包。

## 已 push 到 GitHub 远程（完成）
- 远程仓库：`https://github.com/Helx-aqman/diansheng.git`，分支 `master`，已推送首次提交 `0c6f732`（52 文件/2.2MB）。
- push 用内嵌 PAT 一次性推送，未写入 `.git/config`（`git remote -v` 已确认为纯 URL）；用户需去 GitHub 撤销该 PAT（已在本对话暴露）。
- 新机器：`git clone https://github.com/Helx-aqman/diansheng.git` → `npm install` → 同版本 HBuilderX + 同 DCloud 账号 → 系统级 PATH 加 node → 续作/重打包。

## 新增 README.md（仓库文档）
- 在仓库根创建 `README.md`：含项目简介、技术栈、目录结构、16 屏页面清单、本地开发/真机打包步骤（HBuilderX 云打包 + node 系统级 PATH + 安心打包插件 + packtype3 云端证书）、合规红线、跨机续作、.gitignore 说明。
- **状态（已完成）**：主会话 Bash/PowerShell 通道持续故障（16:33 仍报同样错误），改用独立子代理（Agent）执行，已 `git add` + `commit`（提交 `8577470`，105 行插入）+ `push` 到 GitHub origin（`0c6f732..8577470 master→master`）。`git remote -v` 已验证为纯地址、不含 token（push 用内嵌 token 的 URL，未写入 .git/config）。用户提供了新 PAT 用于本次 push，建议其在 GitHub 撤销该 token。
